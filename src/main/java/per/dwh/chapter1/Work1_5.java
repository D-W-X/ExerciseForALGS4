package per.dwh.chapter1;

/**
 * @author DW
 * @date 2018/11/29
 */

class Work1_5 {
    static boolean testDouble(double a, double b) {
        return (a > 0) && (a < 1) && (b > 0) && (b < 1);
    }
}
